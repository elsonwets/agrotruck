import { Building2, MapPinned, Route, Truck } from "lucide-react";
const stats = [["48", "Camions disponibles", Truck], ["126", "Propriétaires inscrits", Building2], ["8", "Zones couvertes", MapPinned], ["340+", "Demandes traitées", Route]] as const;
export function StatisticsSection() { return <section className="page-shell relative z-10 -mt-20"><div className="glass-panel grid overflow-hidden rounded-2xl bg-white/90 sm:grid-cols-2 lg:grid-cols-4">{stats.map(([value,label,Icon],i)=><div key={label} className={`flex items-center gap-4 p-6 ${i ? "border-t border-primary/10 sm:border-l sm:border-t-0" : ""}`}><span className="grid size-11 shrink-0 place-items-center rounded-xl bg-primary/10 text-primary"><Icon className="size-5"/></span><div><strong className="font-heading text-2xl font-extrabold text-primary">{value}</strong><p className="text-xs text-[#6e685e]">{label}</p></div></div>)}</div></section>; }
